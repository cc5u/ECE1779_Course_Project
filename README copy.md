
  # Create UI from Description

  This is a code bundle for Create UI from Description. The original project is available at https://www.figma.com/design/uwZjAV4ygux2G8AolNaKWJ/Create-UI-from-Description.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  